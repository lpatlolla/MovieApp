<%= form_for(@casting) do |f| %>
  <% if @casting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@casting.errors.count, "error") %> prohibited this casting from being saved:</h2>

      <ul>
      <% @casting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %><br>
    <%= f.number_field :movie_id %>
  </div>
  <div class="field">
    <%= f.label :actor_id %><br>
    <%= f.number_field :actor_id %>
  </div>
  <div class="field">
    <%= f.label :ord %><br>
    <%= f.text_field :ord %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
